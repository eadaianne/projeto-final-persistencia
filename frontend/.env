# Vite environment placeholders for local development
# Backend API base URL used by the frontend (update if your backend runs elsewhere)
VITE_API_URL=http://localhost:8080

# Base URL for uploaded files (images, etc.)
VITE_UPLOAD_URL=http://localhost:8080/uploads

# App environment (development|production)
VITE_APP_ENV=development
